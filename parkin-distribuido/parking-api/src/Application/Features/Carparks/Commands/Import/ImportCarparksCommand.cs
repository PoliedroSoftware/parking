//------------------------------------------------------------------------------
// <auto-generated>
// CleanArchitecture.Blazor - MIT Licensed.
// Author: neozhu
// Created/Modified: 2025-10-17
// Import command & template for carparks.
// Validates Excel data, prevents duplicates, and provides a template for bulk entry.
// Docs: https://docs.cleanarchitectureblazor.com/features/carpark
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
#nullable disable warnings

using CleanArchitecture.Blazor.Application.Features.Carparks.DTOs;
using CleanArchitecture.Blazor.Application.Features.Carparks.Caching;

namespace CleanArchitecture.Blazor.Application.Features.Carparks.Commands.Import;

    public class ImportCarparksCommand: ICacheInvalidatorRequest<Result<int>>
    {
        public string FileName { get; set; }
        public byte[] Data { get; set; }
        public string CacheKey => CarparkCacheKey.GetAllCacheKey;
        public IEnumerable<string>? Tags => CarparkCacheKey.Tags;
        public ImportCarparksCommand(string fileName,byte[] data)
        {
           FileName = fileName;
           Data = data;
        }
    }
    public record class CreateCarparksTemplateCommand : IRequest<Result<byte[]>>
    {
 
    }

    public class ImportCarparksCommandHandler : 
                 IRequestHandler<CreateCarparksTemplateCommand, Result<byte[]>>,
                 IRequestHandler<ImportCarparksCommand, Result<int>>
    {
        private readonly IApplicationDbContextFactory _dbContextFactory;
        private readonly IStringLocalizer<ImportCarparksCommandHandler> _localizer;
        private readonly IExcelService _excelService;
        private readonly CarparkDto _dto = new();
        private readonly IMapper _mapper;
        public ImportCarparksCommandHandler(
            IApplicationDbContextFactory dbContextFactory,
            IMapper mapper,
            IExcelService excelService,
            IStringLocalizer<ImportCarparksCommandHandler> localizer)
        {
            _dbContextFactory = dbContextFactory;
            _localizer = localizer;
            _excelService = excelService;
            _mapper = mapper;
        }
        #nullable disable warnings
        public async Task<Result<int>> Handle(ImportCarparksCommand request, CancellationToken cancellationToken)
        {
           await using var db = await _dbContextFactory.CreateAsync(cancellationToken);
           var result = await _excelService.ImportAsync(request.Data, mappers: new Dictionary<string, Func<DataRow, CarparkDto, object?>>
            {
                                { _localizer[_dto.GetMemberDescription(x=>x.AppKey)], (row, item) => item.AppKey = row[_localizer[_dto.GetMemberDescription(x=>x.AppKey)]].ToString() }, 
                { _localizer[_dto.GetMemberDescription(x=>x.MachineCode)], (row, item) => item.MachineCode = row[_localizer[_dto.GetMemberDescription(x=>x.MachineCode)]].ToString() }, 
                { _localizer[_dto.GetMemberDescription(x=>x.RegistrationCode)], (row, item) => item.RegistrationCode = row[_localizer[_dto.GetMemberDescription(x=>x.RegistrationCode)]].ToString() }, 
                { _localizer[_dto.GetMemberDescription(x=>x.ContactPerson)], (row, item) => item.ContactPerson = row[_localizer[_dto.GetMemberDescription(x=>x.ContactPerson)]].ToString() }, 
                { _localizer[_dto.GetMemberDescription(x=>x.PhoneNumber)], (row, item) => item.PhoneNumber = row[_localizer[_dto.GetMemberDescription(x=>x.PhoneNumber)]].ToString() }, 
                { _localizer[_dto.GetMemberDescription(x=>x.Fax)], (row, item) => item.Fax = row[_localizer[_dto.GetMemberDescription(x=>x.Fax)]].ToString() }, 
                { _localizer[_dto.GetMemberDescription(x=>x.Email)], (row, item) => item.Email = row[_localizer[_dto.GetMemberDescription(x=>x.Email)]].ToString() }, 
                { _localizer[_dto.GetMemberDescription(x=>x.Description)], (row, item) => item.Description = row[_localizer[_dto.GetMemberDescription(x=>x.Description)]].ToString() }, 
                { _localizer[_dto.GetMemberDescription(x=>x.Identified)], (row, item) => item.Identified = row[_localizer[_dto.GetMemberDescription(x=>x.Identified)]].ToString() }, 
                { _localizer[_dto.GetMemberDescription(x=>x.TenantId)], (row, item) => item.TenantId = row[_localizer[_dto.GetMemberDescription(x=>x.TenantId)]].ToString() }, 

            }, _localizer[_dto.GetClassDescription()]);
            if (result.Succeeded && result.Data is not null)
            {
                foreach (var dto in result.Data)
                {
                    var exists = await db.Carparks.AnyAsync(x => x.AppKey == dto.AppKey, cancellationToken);
                    if (!exists)
                    {
                        var item = _mapper.Map<Carpark>(dto);
                        // add create domain events if this entity implement the IHasDomainEvent interface
				        // item.AddDomainEvent(new CarparkCreatedEvent(item));
                        await db.Carparks.AddAsync(item, cancellationToken);
                    }
                 }
                 await db.SaveChangesAsync(cancellationToken);
                 return await Result<int>.SuccessAsync(result.Data.Count());
           }
           else
           {
               return await Result<int>.FailureAsync(result.Errors);
           }
        }
        public async Task<Result<byte[]>> Handle(CreateCarparksTemplateCommand request, CancellationToken cancellationToken)
        {
            var fields = new string[] {
                   _localizer[_dto.GetMemberDescription(x=>x.AppKey)], 
_localizer[_dto.GetMemberDescription(x=>x.MachineCode)], 
_localizer[_dto.GetMemberDescription(x=>x.RegistrationCode)], 
_localizer[_dto.GetMemberDescription(x=>x.ContactPerson)], 
_localizer[_dto.GetMemberDescription(x=>x.PhoneNumber)], 
_localizer[_dto.GetMemberDescription(x=>x.Fax)], 
_localizer[_dto.GetMemberDescription(x=>x.Email)], 
_localizer[_dto.GetMemberDescription(x=>x.Description)], 
_localizer[_dto.GetMemberDescription(x=>x.Identified)], 
_localizer[_dto.GetMemberDescription(x=>x.TenantId)], 

                };
            var result = await _excelService.CreateTemplateAsync(fields, _localizer[_dto.GetClassDescription()]);
            return await Result<byte[]>.SuccessAsync(result);
        }
    }

